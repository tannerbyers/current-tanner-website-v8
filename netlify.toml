[build]
  command = "npm run build"
  publish = "_site"

# Disable legacy Netlify prerendering in the Netlify UI for this project.
# This site is fully generated by Eleventy, so the static output in _site is the artifact to publish.

# If you later adopt the new Netlify prerender extension, the plugin block can be added here.
# [[plugins]]
#   package = "@netlify/plugin-prerender"
